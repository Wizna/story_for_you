You produced malformed JSON during the story event extraction task. Convert the
invalid output below into a syntactically valid JSON array of PlotEvent objects.

Schema: each element is `{ event_id: string, chapter: int, type: string ("progress"|"setback"|"reveal"|"conflict"), participants: string[], summary: string, impact: { power_shifts: {string: string}, relation_changes: {string: string}, world_flags: string[] }, is_irreversible: bool }`. Supply sensible defaults for missing fields (empty list, false, etc.). Preserve the language/tone of the original descriptions.

Reason the previous output was rejected:
{{error_message}}

Invalid output to fix (truncated if necessary):
{{invalid_output}}

Return ONLY the corrected JSON array — no code fences, no commentary.
